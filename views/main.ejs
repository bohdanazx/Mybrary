<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Mybrary</title>
    <link rel="stylesheet" href="/stylesheets/mainAuth.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <style>
        :root {
            font-family: 'Gotham Rounded';
            font-size: 20px;
            color: var(--color-text-light);
        }

        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(to right, var(--color-background-dark), var(--color-background-light));
            margin: 0;
            padding: 0;
            font-family: 'Gotham Rounded', sans-serif;
            color: var(--color-text-light);
            font-size: 20px;
        }

        .container {
            max-width: 1200px;
            margin: auto;
            margin-bottom: 2rem;
            padding: 0 20px;
        }

        .welcome {
            color: white;
            font-size: 3rem;
            text-align: center;
            margin-top: 5vh;
        }

        h4 {
            text-align: center;
            margin-top: 2rem;
        }

        .auth {
            display: flex;
            justify-content: center;
            align-items: center;
            list-style-type: none;
            padding: 0;
            margin-top: 2rem;
        }

        .auth li {
            margin: 0 1rem;
        }

        .btn-auth {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            font-size: 1.2rem;
            border-radius: 0.5rem;
            background-color: var(--color-btn-primary-background);
            color: var(--color-btn-primary-text);
            text-decoration: none;
            text-align: center;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .btn-auth:hover {
            background-color: var(--color-btn-primary-background-dark);
            color: var(--color-btn-primary-text-dark);
        }

        .btn-primary {
            display: flex;
            justify-content: center;
            margin-top: 2rem;
            text-align: center;
        }

        .btn-primary a {
            display: inline-block;
            padding: 1rem 2rem;
            font-size: 1.2rem;
            border-radius: 0.5rem;
            background-color: var(--color-btn-primary-background);
            /* Замініть на ваше значення */
            color: var(--color-btn-primary-text);
            /* Замініть на ваше значення */
            text-decoration: none;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .btn-primary a:hover {
            background-color: var(--color-btn-primary-background-dark);
            color: var(--color-btn-primary-text-dark);
        }
    </style>
</head>

<body>
    <div class="container">
        <h2 class="welcome">Welcome to Mybrary</h2>
        <h4>Your personal online library. Manage your book collection and keep track of your favorite authors.</h4>
        <ul class="auth">
            <li><a class="btn-auth" href="/login">Log in</a></li>
            <li><a class="btn-auth" href="/signup">Sign up</a></li>
        </ul>
        <div class="btn-primary">
            <a href="/library" class="btn-primary-link">Go to library</a>
        </div>
    </div>
</body>

</html>